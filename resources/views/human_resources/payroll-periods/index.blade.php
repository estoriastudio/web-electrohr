@extends('layouts.app')
@section('page_title', 'Periodos de nómina')
@section('breadcrumbs')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">Inicio</a></li><li class="breadcrumb-item active">Nómina</li>@endsection
@section('content')
@include('human_resources.partials.flash')
<div class="row g-3"><div class="col-lg-4"><div class="card"><div class="card-header"><h5 class="card-title mb-0">Nuevo periodo</h5></div><div class="card-body"><form method="POST" action="{{ route('human_resources.payroll-periods.store') }}">@csrf<label class="form-label">Viernes inicial</label><input name="start_date" type="date" class="form-control @error('start_date') is-invalid @enderror" value="{{ old('start_date') }}" required>@error('start_date')<div class="invalid-feedback">{{ $message }}</div>@enderror<label class="form-label mt-3">Notas</label><textarea name="notes" class="form-control" rows="3">{{ old('notes') }}</textarea><button class="btn btn-primary w-100 mt-3">Crear periodo</button></form></div></div></div><div class="col-lg-8"><div class="card"><div class="card-header"><h5 class="card-title mb-0">Periodos</h5></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="bg-light-subtle"><tr><th>Semana</th><th>Rango</th><th>Líneas</th><th>Estatus</th><th class="text-end">Acciones</th></tr></thead><tbody>@forelse($payrollPeriods as $period)<tr><td class="fw-medium">{{ $period->week_number }}/{{ $period->year }}</td><td>{{ $period->start_date->format('d/m/Y') }} - {{ $period->end_date->format('d/m/Y') }}</td><td>{{ $period->lines_count }}</td><td><span class="badge {{ $period->status === 'open' ? 'bg-success-subtle text-success' : ($period->status === 'closed' ? 'bg-warning-subtle text-warning' : 'bg-primary-subtle text-primary') }}">{{ ['open' => 'Abierto', 'closed' => 'Cerrado', 'paid' => 'Pagado'][$period->status] }}</span></td><td class="text-end"><a class="btn btn-light btn-sm" href="{{ route('human_resources.payroll-lines.index', ['payroll_period_id' => $period->id]) }}" title="Ver líneas"><i class="ri-list-check-2"></i></a>@if($period->status === 'open')<form class="d-inline" method="POST" action="{{ route('human_resources.payroll-periods.generate-lines', $period) }}">@csrf<button class="btn btn-light btn-sm" title="Generar líneas"><i class="ri-play-line"></i></button></form><form class="d-inline" method="POST" action="{{ route('human_resources.payroll-periods.close', $period) }}">@csrf<button class="btn btn-light btn-sm text-warning" title="Cerrar"><i class="ri-lock-line"></i></button></form>@elseif($period->status === 'closed')<form class="d-inline" method="POST" action="{{ route('human_resources.payroll-periods.mark-paid', $period) }}">@csrf<button class="btn btn-light btn-sm text-success" title="Marcar pagado"><i class="ri-checkbox-circle-line"></i></button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No hay periodos de nómina.</td></tr>@endforelse</tbody></table></div>@if($payrollPeriods->hasPages())<div class="card-footer">{{ $payrollPeriods->links('pagination::bootstrap-5') }}</div>@endif</div></div></div>
@endsection