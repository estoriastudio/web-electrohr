@php($workerVacation = $workerVacation ?? null)
<div class="row g-3"><div class="col-md-6"><label class="form-label" for="worker_id">Trabajador</label><select id="worker_id" name="worker_id" class="form-select" required>@foreach($workers as $worker)<option value="{{ $worker->id }}" @selected((string) old('worker_id', $workerVacation?->worker_id) === (string) $worker->id)>{{ $worker->last_name }}, {{ $worker->first_name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label" for="request_date">Fecha de solicitud</label><input id="request_date" name="request_date" type="date" class="form-control" value="{{ old('request_date', $workerVacation?->request_date?->format('Y-m-d') ?? now()->format('Y-m-d')) }}" required></div><div class="col-md-6"><label class="form-label" for="start_date">Inicio</label><input id="start_date" name="start_date" type="date" class="form-control" value="{{ old('start_date', $workerVacation?->start_date?->format('Y-m-d')) }}" required></div><div class="col-md-6"><label class="form-label" for="end_date">Fin</label><input id="end_date" name="end_date" type="date" class="form-control" value="{{ old('end_date', $workerVacation?->end_date?->format('Y-m-d')) }}" required></div>@if($workerVacation)<div class="col-md-6"><label class="form-label" for="status">Estatus</label><select id="status" name="status" class="form-select" required>@foreach(['in_progress' => 'En curso', 'approved' => 'Aprobada', 'taken' => 'Tomada', 'cancelled' => 'Cancelada'] as $value => $label)<option value="{{ $value }}" @selected(old('status', $workerVacation->status) === $value)>{{ $label }}</option>@endforeach</select></div>@endif<div class="col-12"><label class="form-label" for="notes">Notas</label><textarea id="notes" name="notes" class="form-control" rows="3">{{ old('notes', $workerVacation?->notes) }}</textarea></div></div>